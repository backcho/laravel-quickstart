# 06. 보너스: UI 커스텀

> 목표: laravel/ui Bootstrap 템플릿 커스터마이징

AI가 기능을 만들어줬다면, 이제 디자인을 입힐 차례입니다. laravel/ui의 Bootstrap 템플릿 구조를 이해하고 커스터마이징하는 방법을 알아봅니다.

---

## 1. 템플릿 구조 이해

### 메인 레이아웃

```
resources/views/layouts/app.blade.php
```

이 파일이 모든 페이지의 기본 틀입니다.

```blade
<!doctype html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>{{ config('app.name', 'Laravel') }}</title>
    
    @vite(['resources/sass/app.scss', 'resources/js/app.js'])
</head>
<body>
    <div id="app">
        {{-- 네비게이션 바 --}}
        @include('layouts.navigation')
        
        <main class="py-4">
            @yield('content')  {{-- 각 페이지 내용이 들어가는 곳 --}}
        </main>
        
        {{-- 푸터 (추가) --}}
        @include('layouts.footer')
    </div>
</body>
</html>
```

### Blade 문법 기초

```blade
{{-- 레이아웃 상속 --}}
@extends('layouts.app')

{{-- 섹션 정의 --}}
@section('content')
    <h1>페이지 내용</h1>
@endsection

{{-- 섹션 삽입 위치 --}}
@yield('content')

{{-- 컴포넌트 포함 --}}
@include('components.alert')

{{-- 조건문 --}}
@if($condition)
    ...
@endif

{{-- 반복문 --}}
@foreach($items as $item)
    ...
@endforeach
```

### 파일 구조

```
resources/views/
├── layouts/
│   ├── app.blade.php      # 메인 레이아웃
│   ├── navigation.blade.php  # 네비게이션 (분리 시)
│   └── footer.blade.php   # 푸터 (추가)
├── auth/
│   ├── login.blade.php
│   ├── register.blade.php
│   └── passwords/
│       ├── email.blade.php
│       └── reset.blade.php
├── posts/
│   ├── index.blade.php
│   ├── show.blade.php
│   ├── create.blade.php
│   └── edit.blade.php
└── home.blade.php
```

---

## 2. 기본 커스터마이징

### 네비게이션 바 수정

```blade
{{-- resources/views/layouts/app.blade.php 내 navbar 부분 --}}

<nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm">
    <div class="container">
        {{-- 로고/브랜드 --}}
        <a class="navbar-brand" href="{{ url('/') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" height="30">
            {{ config('app.name', 'Laravel') }}
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
                data-bs-target="#navbarSupportedContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            {{-- 왼쪽 메뉴 --}}
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ request()->routeIs('posts.*') ? 'active' : '' }}" 
                       href="{{ route('posts.index') }}">
                        게시판
                    </a>
                </li>
            </ul>

            {{-- 오른쪽 메뉴 --}}
            <ul class="navbar-nav ms-auto">
                @guest
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('login') }}">로그인</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('register') }}">회원가입</a>
                    </li>
                @else
                    {{-- 관리자 메뉴 --}}
                    @if(auth()->user()->hasRole('admin'))
                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('admin.dashboard') }}">
                                관리자
                            </a>
                        </li>
                    @endif
                    
                    {{-- 사용자 드롭다운 --}}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" 
                           data-bs-toggle="dropdown">
                            {{ Auth::user()->name }}
                        </a>
                        <div class="dropdown-menu dropdown-menu-end">
                            <a class="dropdown-item" href="{{ route('logout') }}"
                               onclick="event.preventDefault();
                                        document.getElementById('logout-form').submit();">
                                로그아웃
                            </a>
                            <form id="logout-form" action="{{ route('logout') }}" 
                                  method="POST" class="d-none">
                                @csrf
                            </form>
                        </div>
                    </li>
                @endguest
            </ul>
        </div>
    </div>
</nav>
```

### 푸터 추가

```blade
{{-- resources/views/layouts/footer.blade.php --}}

<footer class="bg-light py-4 mt-auto">
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <p class="mb-0 text-muted">
                    &copy; {{ date('Y') }} {{ config('app.name') }}. All rights reserved.
                </p>
            </div>
            <div class="col-md-6 text-md-end">
                <a href="#" class="text-muted me-3">이용약관</a>
                <a href="#" class="text-muted">개인정보처리방침</a>
            </div>
        </div>
    </div>
</footer>
```

### 알림 메시지 컴포넌트

```blade
{{-- resources/views/components/alert.blade.php --}}

@if(session('success'))
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        {{ session('success') }}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
@endif

@if(session('error'))
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        {{ session('error') }}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
@endif

@if($errors->any())
    <div class="alert alert-danger">
        <ul class="mb-0">
            @foreach($errors->all() as $error)
                <li>{{ $error }}</li>
            @endforeach
        </ul>
    </div>
@endif
```

레이아웃에서 사용:

```blade
<main class="py-4">
    <div class="container">
        @include('components.alert')
    </div>
    @yield('content')
</main>
```

---

## 3. CSS 커스터마이징

### SCSS 구조

```
resources/
├── sass/
│   ├── app.scss          # 메인 SCSS
│   └── _variables.scss   # Bootstrap 변수 오버라이드
```

### Bootstrap 변수 커스터마이징

```scss
// resources/sass/_variables.scss

// 색상 커스터마이징
$primary: #5c6bc0;       // 기본 색상
$secondary: #78909c;
$success: #66bb6a;
$info: #29b6f6;
$warning: #ffa726;
$danger: #ef5350;

// 폰트
$font-family-sans-serif: 'Pretendard', -apple-system, BlinkMacSystemFont, 
                         'Segoe UI', Roboto, sans-serif;

// 둥근 모서리
$border-radius: 0.5rem;
$border-radius-lg: 0.75rem;
$border-radius-sm: 0.25rem;

// 그림자
$box-shadow-sm: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.05);
$box-shadow: 0 0.25rem 0.5rem rgba(0, 0, 0, 0.1);
```

```scss
// resources/sass/app.scss

// 변수 먼저 import (Bootstrap 변수 오버라이드)
@import 'variables';

// Bootstrap
@import 'bootstrap/scss/bootstrap';

// 커스텀 스타일
body {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

main {
    flex: 1;
}

// 카드 커스텀
.card {
    border: none;
    box-shadow: $box-shadow-sm;
    transition: box-shadow 0.2s ease;
    
    &:hover {
        box-shadow: $box-shadow;
    }
}

// 버튼 커스텀
.btn {
    font-weight: 500;
}
```

### 빌드

```bash
npm run build    # 프로덕션 빌드
npm run dev      # 개발 모드 (watch)
```

---

## 4. Blade 컴포넌트 만들기

### 버튼 컴포넌트

```blade
{{-- resources/views/components/button.blade.php --}}

@props([
    'type' => 'button',
    'variant' => 'primary',
    'size' => 'md'
])

<button 
    type="{{ $type }}" 
    {{ $attributes->merge(['class' => "btn btn-{$variant} btn-{$size}"]) }}
>
    {{ $slot }}
</button>
```

사용법:

```blade
<x-button type="submit" variant="success">저장</x-button>
<x-button variant="danger" onclick="confirmDelete()">삭제</x-button>
```

### 카드 컴포넌트

```blade
{{-- resources/views/components/card.blade.php --}}

@props(['title' => null])

<div {{ $attributes->merge(['class' => 'card']) }}>
    @if($title)
        <div class="card-header">
            <h5 class="card-title mb-0">{{ $title }}</h5>
        </div>
    @endif
    <div class="card-body">
        {{ $slot }}
    </div>
</div>
```

사용법:

```blade
<x-card title="게시글 작성">
    <form>
        ...
    </form>
</x-card>
```

---

## 5. AI에게 디자인 요청하기

### 효과적인 프롬프트 예시

#### 레이아웃 변경

```
현재 Laravel 프로젝트의 layouts/app.blade.php를 다음과 같이 수정해주세요:

1. 네비게이션 바
   - 왼쪽: 로고 + 메인 메뉴 (홈, 게시판)
   - 오른쪽: 로그인/회원가입 또는 사용자 드롭다운
   - 관리자인 경우 "관리자" 메뉴 추가
   - 현재 페이지 메뉴 하이라이트

2. 푸터
   - 저작권, 이용약관, 개인정보처리방침 링크
   - 하단에 고정 (sticky footer)

3. 알림 메시지
   - success, error 세션 메시지 표시
   - 닫기 버튼 포함
```

#### 스타일 변경

```
Bootstrap 테마를 다음과 같이 커스터마이징해주세요:

색상 테마:
- Primary: #4F46E5 (인디고)
- 전체적으로 모던하고 깔끔한 느낌

스타일:
- 카드에 그림자 추가
- 버튼 둥글게 (border-radius 늘리기)
- 폰트: Pretendard (웹폰트 적용)
```

#### 다크 테마 추가

```
다크 테마를 추가해주세요:

1. 토글 버튼으로 라이트/다크 전환
2. 로컬 스토리지에 선택 저장
3. 시스템 설정 자동 감지 (prefers-color-scheme)
4. Bootstrap 5의 data-bs-theme 활용
```

#### 참고 이미지/사이트 제공

```
아래 사이트를 참고해서 게시판 목록 페이지를 리디자인해주세요:
- 참고: [사이트 URL 또는 스크린샷 설명]

원하는 요소:
- 카드형 목록
- 호버 시 애니메이션
- 작성자 아바타 표시
- 태그 뱃지
```

---

## 6. 유용한 리소스

### 무료 UI 키트

- [Bootstrap Examples](https://getbootstrap.com/docs/5.3/examples/)
- [Bootswatch](https://bootswatch.com/) - Bootstrap 테마
- [Start Bootstrap](https://startbootstrap.com/) - 무료 템플릿

### 아이콘

```blade
{{-- Bootstrap Icons --}}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

{{-- 사용 --}}
<i class="bi bi-house"></i>
<i class="bi bi-person"></i>
<i class="bi bi-pencil"></i>
```

### 웹폰트 (Pretendard)

```html
{{-- resources/views/layouts/app.blade.php의 head에 추가 --}}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css">
```

```scss
// resources/sass/_variables.scss
$font-family-sans-serif: 'Pretendard', -apple-system, sans-serif;
```

---

## 마무리

이 가이드를 통해 Laravel로 완전한 MVP를 만들 수 있습니다:

1. ✅ PHP 개발 환경 구축
2. ✅ Laravel 프로젝트 생성
3. ✅ 인증 + 게시판 구현
4. ✅ 관리자 백오피스 (RBAC)
5. ✅ ngrok으로 외부 공개
6. ✅ UI 커스터마이징

**Next.js + Vercel "딸깍"과 다른 점?**

Laravel은 "진짜 백엔드"입니다. 인증, 권한, 큐, 스케줄링, 이메일... 웹 애플리케이션에 필요한 모든 것이 포함되어 있습니다.

바이브 코딩으로 시작해서, 진짜 서비스로 성장시키세요! 🚀

---

## 추가 학습 자료

- [Laravel 공식 문서 (한글)](https://laravel.kr/docs)
- [Laracasts](https://laracasts.com/) - Laravel 동영상 강좌
- [Laravel News](https://laravel-news.com/) - 최신 소식
